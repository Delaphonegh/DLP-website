---
interface Props { compact?: boolean }
const { compact = false } = Astro.props;
---
<span class:list={["brand", { compact }]}><i class="brand-mark" aria-hidden="true">{Array.from({ length: 8 }, (_, i) => <b data-dot={i} />)}</i>{!compact && <strong>DLP</strong>}</span>
